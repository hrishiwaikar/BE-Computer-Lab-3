This is a concurrent quicksort assignment 

There are three files
1. Serial quicksort => Program that runs serial quicksort
2. Parallel quicksort => Program that runs quicksort parallely using multithreading
3. Quicksort comparison => Program that generates a random list of given size and sorts it using the above two quicksort and compares 
				the performance

Run the third file , thats all !

